language: crystal

services:
  - docker 

install:
  - docker pull gabfitzgerald/hello-place
  - docker run -d gabfitzgerald/hello-place
  - docker run -p 5432:8000 -e POSTGRES_PASSWORD=postgres postgres
  - shards install

script:
  - crystal spec
  - bin/ameba
