language: crystal

services:
  - docker 

install:
  - docker pull gabfitzgerald/latest
  - docker run -d -p 2222:22 -e ROOT_PASS="PASSWORD" gabfitzgerald/latest
  - shards install

script:
  - crystal spec
  - bin/ameba
